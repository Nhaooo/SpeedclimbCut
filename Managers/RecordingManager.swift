import Foundation
import CoreMedia

class RecordingManager {
    // This could handle local caching and managing recordings
    // Currently handled natively in CameraManager for MVP scope.
    // Useful for a V2 structured filesytem approach.
}
